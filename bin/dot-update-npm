#!/usr/bin/env bash

f="npm-global-packages.txt"
rm "$f" 2>/dev/null

# list all the gloablly installed npm packages in parseable format
# -p is parseable format
npm list -g -p | awk -F'node_modules/' 'NF > 1 {print $2}' > "$f"
